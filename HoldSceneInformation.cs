using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HoldSceneInformation : MonoBehaviour {

	// Use this for initialization
	void Start () {
		print(GlobalControl.Instance.id);
	}
	
	// Update is called once per frame
	void Update () {
		
	}


}
